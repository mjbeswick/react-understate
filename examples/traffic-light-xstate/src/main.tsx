import React from 'react';
import ReactDOM from 'react-dom/client';
import TrafficLightWidget from './TrafficLightWidget.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TrafficLightWidget />
  </React.StrictMode>
);
